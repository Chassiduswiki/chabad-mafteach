# Production Environment Variables
# Copy this file to .env.production and fill in the actual values
# NEVER commit .env.production to git

# Public URLs (safe to expose)
NEXT_PUBLIC_BASE_URL=https://your-domain.com
NEXT_PUBLIC_APP_URL=https://your-domain.com
NEXT_PUBLIC_DIRECTUS_URL=https://your-directus-instance.com

# Directus Configuration (SENSITIVE - keep secret)
DIRECTUS_URL=https://your-directus-instance.com
DIRECTUS_STATIC_TOKEN=your_directus_static_token_here

# JWT Secret for session tokens (SENSITIVE - generate with: openssl rand -base64 32)
JWT_SECRET=your_jwt_secret_here

# AI Service Keys (SENSITIVE - keep secret)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional: Google Analytics
GOOGLE_SITE_VERIFICATION=your_google_verification_code
